it just...works

really

It displays a triangle with a gradient of different colors (red, green, blue).
The triangle is defined as three vertices (of the class Vertex), that declare their coordinates and color as 5 floats.